[package]
name = "harness-mobile-cdylib"
version = "0.0.1"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
dioxus = { workspace = true, features = ["mobile"] }


[features]

    